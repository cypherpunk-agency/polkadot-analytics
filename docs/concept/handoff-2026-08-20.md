# Handoff — state as of 2026-08-20 ~11:35 Vienna

Written minutes before the local machine shut down, so the next session (local or remote)
starts from the repo alone. Everything below is pushed.

> ### ⚠️ Superseded in most of its particulars — audited 2026-08-21
>
> This is a **dated snapshot**, kept because it records the state a session started from, not
> because it is current. A full day of work landed on top of it. Specifically:
>
> - "**Nine pages, seven sources / 24+ operations**" → **ten dashboards plus a drill-down tool**,
>   and a source registry that grew past thirty operations and gained two store-backed jobs on
>   2026-08-20. Do not transcribe the counts: `npm run check` prints them on every run and `/api`
>   is the machine-readable version, both generated from `server/sources/index.mjs`.
> - "**No source module declares a `jobs` entry yet**" → two do: `hydration/swaps-daily` and
>   `asset-hub/netflows-daily`. See [jobs.md](../architecture/jobs.md).
> - Outstanding **item 2** (`/api` does not list jobs-only operations) — **done**; `describe()`
>   walks `source.jobs` and lists them separately, with a comment saying why folding them in beside
>   the cached operations was worse.
> - Outstanding **item 3** (first real job handlers, then netflows v2) — **done, both halves.**
> - Outstanding **item 4** (retention/eviction, and the infra asks) — the **size** is settled at
>   1 GB on a measured fill rate (plan §12.2); the **ask** is now itemised as item 7 of
>   [deployment.md](../architecture/deployment.md#what-is-still-owed) and tracked as research queue
>   O46. Eviction is still unbuilt and still deliberate.
> - Outstanding **item 1**, hero-stat truncation — **fixed.** `.stat` is a query container and its
>   value is `min(<viewport step>, --fit-value)`, so the number is sized against its own tile
>   rather than the viewport; `src/design/components.css` carries the reasoning, including why the
>   cap sits inside an `@supports` block. A *different* mobile overflow found later — `.rank-row`
>   overflowing to 423px at 390px — was fixed on 2026-08-21 in `00f85d0` and left behind a trap
>   worth reading in CLAUDE.md: `overflow-y: auto` clips horizontally too, so two of the three
>   affected pages measured clean.
> - Outstanding **item 6**, the Bulletin `RETENTION_BLOCKS` note — still open, and still recorded
>   in CLAUDE.md's facts list. The other two QA leftovers in that item were not re-checked here.
>
> **The current list of what is open is `research-queue.md`, and the current account of what is
> built is `plan.md` §9 and §11–12.** Item 5 (Wave D — Bulletin `bitswap_v1_get` and the scoped
> `connect-src` ask) is the part of this page still worth reading as live: nothing in the tree
> mentions `bitswap`.

## Live

analytics.cypherpunk.agency serves build `ba4c4f7` (or later — push-to-deploy is ACTIVE:
every green push to `main` deploys via the gated GitHub Actions job, IAP-tunneled,
digest-pinned). Infra declared mission-complete: monitoring rows + GCP uptime checks are
wired, DR manifest updated, stack snapshots in sync. The agent bridge file is cold.

## What shipped today

- The whole site: nine pages, seven sources / 24+ operations, knowledge base.
- Wave B complete (all seven lanes, integrated, visually QA'd).
- The job system (store / queue / worker / CLI, `docs/architecture/jobs.md`) **and its HTTP
  wiring** (`server/lib/demand.mjs`): store-backed answers, partial envelopes with
  `{coverage, job}`, `/api/jobs/:id`. 43 server tests. No source module declares a `jobs`
  entry yet — the first real handler is the natural next task.
- Decisions 0006 (demand-driven store) and 0007 (Bulletin client-direct + the one planned
  CSP exception). Unlicense. CSP: production header owned by Caddy alone; the app emits it
  only outside production as the local tripwire.

## Outstanding, in priority order

1. **Hero stat truncation below ~1200px — rule-3 violation, live right now.** `.stat-row`
   (`src/design/components.css` ~204) has `overflow:hidden`; `.stat .v.hero` at `--step-4`
   (floor 2.1rem) cuts long money strings MID-NUMBER: /hydration-market/ shows "$55,687,70"
   for $55,687,702. Clipped at 1100/1000/900/760/600/420 px, fine ≥1280. Fix must
   shrink-to-fit (never wrap mid-digits, never truncate) without changing correct wide
   layouts — container-query font sizing is the natural tool. Verify in a browser at all
   those widths on /hydration/, /hydration-market/, /hyperfx/. (A fix agent was mid-flight
   at shutdown on branch `fix/hero-truncation`; if that branch exists on origin, start from
   it, else redo.)
2. `/api` self-description does not list jobs-only operations — `describe()` in
   `server/sources/index.mjs` needs to walk `source.jobs` too. Matters the moment the first
   handler lands.
3. First real job handlers (Hydration backfill is the obvious first), then netflows v2 /
   transfer graph / history / top holders per `docs/concept/plan.md` Wave C.
4. Store retention/eviction is deliberately absent — make it a stated decision when the
   store ships to production, together with the infra asks: one volume under `/mnt/pd`,
   `ANALYTICS_DATA_DIR`, a `VACUUM INTO`-style checkpoint hook, measured size (bridge,
   2026-08-20).
5. Wave D: Bulletin `bitswap_v1_get` content browsing + the scoped `connect-src` ask to
   infra (one line on their side, they confirmed).
6. Minor QA leftovers: `/hydration-peg/` renders `-0.000 bps`; `/hydration-wraps/` clamps
   a >100% bar so 560% looks like 100%; CLAUDE.md's Bulletin RETENTION_BLOCKS fact predates
   the live read the bulletin lane shipped.

## Conventions that hold

Opus for most subagents; Workflow only for staged dependencies whose intermediate results
the orchestrator need not read; no local filesystem paths anywhere in the repo (`npm run
check` now fails on them — true of `docs/` when this was written, and true of the whole
repository from 2026-08-20, when the scan was widened and found a hardcoded drive letter in
`scripts/build-netflows-dataset.mjs`); nothing about credentials in writing, ever.
